namespace Core.API.Endpoints
{
    // /api/v1/bootstrap — see USER_API_CONTRACTS.md > Bootstrap (player).
    public static class BootstrapRoutes
    {
        public static readonly string Player = $"{ApiRoutes.V1}/bootstrap";
    }
}
